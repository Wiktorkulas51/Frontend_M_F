import React from "react";
import ChatButton from "./chatButton";

export default {
  title: "Components/atoms/buttons/chatButton",
  component: ChatButton,
};

export const chatButton = () => <ChatButton>Send</ChatButton>;
