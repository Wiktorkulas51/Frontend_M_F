import { Icon, InlineIcon } from "@iconify/react";
import roundLibraryMusic from "@iconify/icons-ic/round-library-music";

const logoIcon = <Icon icon={roundLibraryMusic} />;

export default LogoIcon;
